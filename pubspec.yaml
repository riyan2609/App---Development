name: attendance_app
description: A comprehensive attendance management system
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI & Styling
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  animations: ^2.0.11
  lottie: ^3.0.0
  shimmer: ^3.0.0
  fl_chart: ^0.66.0
  percent_indicator: ^4.2.3
  calendar_view: ^1.0.4
  table_calendar: ^3.0.9

  # State Management
  provider: ^6.1.1
  get: ^4.6.6

  # Firebase
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.2
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.1.4
  firebase_messaging: ^15.0.3

  # Social Login
  google_sign_in: ^6.2.1
  flutter_facebook_auth: ^6.0.4

  # Local Storage
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Media
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1

  # File Generation
  pdf: ^3.10.7
  printing: ^5.11.1
  open_file: ^3.3.2
  path_provider: ^2.1.2
  excel: ^4.0.2

  # Utilities
  intl: ^0.20.2
  uuid: ^4.3.3
  url_launcher: ^6.2.4
  permission_handler: ^11.2.0
  connectivity_plus: ^5.0.2
  http: ^1.2.0
  dio: ^5.4.0

  # Notifications (Only One Plugin — No Conflicts)
  awesome_notifications: ^0.10.1
  timezone: ^0.9.2

  # Authentication Helpers
  pinput: ^4.0.0
  country_code_picker: ^3.0.0
  local_auth: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
